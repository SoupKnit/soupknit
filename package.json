{
  "name": "soupknit-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "pnpm run --parallel -r dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
